# 🎬 Парсинг видео по ссылке (VK / YouTube) — план и варианты

Заметки по загрузке «по ссылке». Задача разбита на два контура — **VK Видео** и
**YouTube (Google)** — потому что ломаются они по разным причинам и лечатся разными
средствами. Здесь фиксируем: что уже сделано в коде, почему могло не работать вообще
(ни приватное, ни публичное), и какие есть варианты дальше.

---

## 1. Текущая реализация (worker.py → `download_url`)

Скачиваем **только аудиодорожку** целиком (`-f bestaudio/best`), фрагменты режем уже
локально ffmpeg'ом (см. `_process`). Раньше клипы вырезались на этапе скачивания через
`--download-sections` — от этого отказались: с несколькими фрагментами таймкоды сбивались,
а сама опция на VK работала нестабильно.

### Каскад попыток (главное исправление доступа)

Ключевая находка: **протухшие / привязанные к IP cookies ломают ДАЖЕ публичные видео** —
VK и YouTube отвергают битую сессию и отвечают «only available for registered users» /
«Sign in to confirm you're not a bot», хотя без cookies ролик скачался бы анонимно.
Раньше код всегда подставлял cookies, если файл присутствовал, — и público-загрузка падала.

Теперь `download_url` пробует по очереди, пока не получится. Для YouTube клиенты
выбраны по [PO-Token-гайду yt-dlp](https://github.com/yt-dlp/yt-dlp/wiki/PO-Token-Guide):
берём только те, что **не требуют PO-token** (`tv`, `web_embedded`, `android_vr`), —
иначе без внешнего провайдера токенов загрузка всё равно упадёт.

| # | VK | YouTube (клиент) |
|---|----|---------|
| 1 | с cookies (если есть) | `default` + cookies (если есть) |
| 2 | **без cookies** (публичный путь) | `default` без cookies |
| 3 | — | `tv` (± cookies) |
| 4 | — | `web_embedded` (± cookies) |
| 5 | — | `android_vr` (± cookies) |

Между попытками чистим недокачанные файлы. Если всё упало — отдаём понятную ошибку
(`friendly_download_error`), которая различает «нужна авторизация» и «устарел yt-dlp / антибот».

### Прочее в коде
- `--retries 3`, `--socket-timeout 30` — устойчивость к сетевым сбоям.
- Прокси per-domain из `data/proxies.yml` (VK — напрямую, YouTube — через прокси).
- deno-рантайм из `data/bin/deno` для n-challenge YouTube.
- Временные файлы (исходник, WAV, `.title`, `.part`) удаляются в `finally` — и при ошибке.

### Что проверить в первую очередь, если «не качает вообще»
1. **Версия yt-dlp** — самая частая причина. VK/YouTube ломают старые версии почти
   каждые пару недель. Обновить: `bash scripts/update-ytdlp.sh` (или `tldw rebuild`).
   Проверить: `docker compose exec worker yt-dlp --version`.
2. **Убрать/переименовать битые cookies** и проверить публичный ролик — если заработало,
   проблема была в cookies (см. § про IP-привязку в OPERATIONS.md).
3. **Ручной прогон в контейнере** для точной диагностики:
   ```bash
   docker compose exec worker yt-dlp -f bestaudio/best --no-playlist -v "<URL>"
   ```
4. **Прокси YouTube** — с российского IP YouTube нестабилен; проверить `data/proxies.yml`.

---

## 2. VK Видео — план

**Публичные ролики** должны качаться `yt-dlp` без cookies (каскад, попытка #2). Если не
качаются даже публичные — почти всегда **устаревший yt-dlp** или сменившийся VK-плеер.

**Приватные / только для авторизованных** — нужны cookies корпоративного аккаунта,
привязанные к IP сервера (SSH-туннель, инструкция в OPERATIONS.md → «Обновление VK-cookies»).

Варианты на будущее (если yt-dlp продолжит ломаться):
- **VK API (video.get)** — официальный, стабильный. Нужен `access_token` приложения с правом
  `video`. Отдаёт прямые ссылки на mp4 разных качеств. Плюс: не зависит от плеера/челленджей.
  Минус: токен и права, приватные видео — только с токеном пользователя, у кого есть доступ.
  → Кандидат №1 для надёжности VK. Прототип: `video.get` по `owner_id`/`video_id` из URL.
- **Собственный извлекатель `m3u8`** из HTML страницы плеера (VK отдаёт HLS-мастер). Хрупко,
  ломается при редизайне плеера — только как крайний фолбэк.
- **gallery-dl** — альтернативный загрузчик, иногда переживает поломки, которые ломают yt-dlp.

---

## 3. YouTube (Google) — план

Основная боль — антибот («Sign in to confirm you're not a bot») и n-challenge (нужен JS-рантайм).
По официальным рекомендациям yt-dlp
([Extractors](https://github.com/yt-dlp/yt-dlp/wiki/Extractors),
[FAQ](https://github.com/yt-dlp/yt-dlp/wiki/FAQ),
[PO-Token-Guide](https://github.com/yt-dlp/yt-dlp/wiki/PO-Token-Guide)) порядок средств такой.

### 3.1. Клиенты без PO-token (уже в каскаде)
`tv`, `web_embedded`, `android_vr` — **не требуют PO-token** и потому единственные, кто реально
качает без cookies и без внешнего провайдера. `web`/`web_safari`/`mweb`/`android`/`ios` требуют
PO-token для потока (GVS) — без bgutil-провайдера (см. 3.4) упадут, в бесток-каскад их не берём.
yt-dlp периодически меняет список клиентов — сверяться с PO-Token-Guide.

### 3.2. Cookies одноразового Google-аккаунта — рекомендуемая процедура yt-dlp
Аккаунт — **отдельный/одноразовый** (не личный, не корпоративный): yt-dlp прямо предупреждает,
что за автоматизацию аккаунт могут забанить. Логиниться нужно через **тот же прокси/IP, что
качает сервер** (IP должен совпадать). Экспорт по инструкции yt-dlp (иначе cookies быстро
протухают — YouTube их ротирует, пока вкладка открыта):

1. Открыть **новое приватное/инкогнито-окно** и войти в YouTube.
2. В **этой же вкладке** перейти на `https://www.youtube.com/robots.txt`
   (чтобы сессия «зафиксировалась» и cookies не ротировались дальше).
3. Экспортировать cookies для `youtube.com` расширением «Get cookies.txt LOCALLY» (Chrome)
   или «cookies.txt» (Firefox) в netscape-формате.
   ⚠️ НЕ использовать одновременно `--cookies` и `--cookies-from-browser` (yt-dlp против).
4. **Закрыть инкогнито-окно, не разлогиниваясь** — logout аннулирует экспортированные cookies.

Положить в `data/cookies/youtube.com.txt` и продублировать в `youtu.be.txt`. Формат — Mozilla/
Netscape (`# Netscape HTTP Cookie File`), перевод строк LF на Unix (CRLF → «HTTP Error 400»).
Рестарт воркера не нужен — cookies читаются на каждом скачивании.

### 3.3. Прокси
Датацентровые/российские IP Google метит быстро. **Резидентный прокси** или собственный **VPS
за рубежом** резко повышают проходимость. Задаётся per-domain в `data/proxies.yml`:
```yaml
youtube.com: "socks5://user:pass@host:port"
youtu.be: "socks5://user:pass@host:port"
```
yt-dlp также советует паузу 5–10 сек между загрузками при массовом скачивании.

### 3.4. PO-token провайдер (когда антибот требует массово)
YouTube всё чаще требует **Proof-of-Origin token**, причём **привязанный к video_id** — на каждый
ролик нужен свежий, вручную это нежизнеспособно. yt-dlp сам токены не генерирует. Решение —
плагин-провайдер **`bgutil-ytdlp-pot-provider`**, который выдаёт токены автоматически. Тогда
станут рабочими и клиенты `web`/`mweb`. Ручной вариант — `--extractor-args "youtube:po_token=..."`
(разово, для отладки). Заложить, когда cookies+прокси перестанут хватать.

### 3.5. Чего НЕ делать
**YouTube Data API v3** метаданные отдаёт, но **не** прямые ссылки на поток — легально скачать
файл через него нельзя. Для нашей задачи (нужна аудиодорожка) API yt-dlp не заменяет.

---

## 4. Общий фолбэк-инструмент (обоих контуров)

- **cobalt** (github.com/imputnet/cobalt) — self-hosted сервис-загрузчик с HTTP API, поддерживает
  и YouTube, и VK. Можно поднять рядом в docker-compose и ходить в него, если yt-dlp упал.
  Кандидат в «второй движок» для устойчивости. Оценить после стабилизации yt-dlp-пути.
- **Единый интерфейс загрузчика** в worker.py: обернуть скачивание в абстракцию
  `fetch_audio(url) -> path` с несколькими бэкендами (yt-dlp → cobalt → VK API), перебор по
  аналогии с текущим каскадом попыток. Рефактор на потом, когда будет ≥2 рабочих бэкенда.

---

## 5. Ближайшие шаги (чек-лист)

- [ ] Обновить yt-dlp на сервере и перепроверить публичный VK + публичный YouTube.
- [ ] Проверить каскад без cookies: VK-public должен качаться сразу; YouTube — как повезёт
      (клиенты `tv`/`web_embedded`/`android_vr` без PO-token).
- [ ] Для приватного VK — переэкспортировать cookies через IP сервера (OPERATIONS.md).
- [ ] YouTube: завести одноразовый Google-аккаунт и экспортировать cookies по процедуре §3.2
      (инкогнито → robots.txt → экспорт → закрыть окно без logout), IP = IP сервера/прокси.
- [ ] Если cookies не хватает — поднять `bgutil-ytdlp-pot-provider` (§3.4) и/или прокси (§3.3).
- [ ] Прототип VK API `video.get` как надёжный бэкенд №2 для VK.
- [ ] Оценить cobalt как self-hosted фолбэк для обоих контуров.
